const extend = (obj, ...objects) => {
  objects.reduce(obj, (orig, o) => {
    for ()
  })
};

class Ctor {
  withPrototype (obj) {
    return _.mixin()
  }
}

const createClass = function(definition){

  const C = () => {

  };

  C.prototype = Object.create(Ctor.prototype);

};